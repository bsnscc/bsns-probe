# Example Markdown Report

Generate a Markdown report with:

```bash
pnpm --filter @bsns/probe-cli dev example.com --markdown
```

The report includes DNS, web reachability, TLS, HTTP header, email authentication, and performance-lite findings. It is generated on demand and is not stored by default.
